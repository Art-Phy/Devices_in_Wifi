# Devices_in_Wifi dependencies
# Python CLI tool for scanning devices in a local network
#
# Install with:
# pip install -r requirements.txt

# Network packet manipulation and ARP scanning
scapy==2.7.0